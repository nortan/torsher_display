# 9. История изменений и решения

Новые записи добавляются сверху.

## PR #6

- Автоперезагрузка экрана через X минут (Оформление → «Работа экрана»): стандартный тег
  `<meta http-equiv="refresh">` в `<head>`, чтобы экран сам восстанавливался после сбоя. Настройка
  `settings.autoRefreshMinutes` (по умолчанию 30, 0 — выключено).
- **Оптимизация анимации экрана:** невидимые слайды не рисуются и не пересчитываются, GPU-слои только
  у активного слайда, тень фото больше не пересчитывается каждый кадр, «свечение» цены анимирует прозрачность,
  фото следующего слайда декодируются заранее. На замедленном CPU «Затухание» — 20 → 55 кадров/с,
  «Куб» и «Переворот» — 1,6 → 33–39, «Колода карт» — 2 → 20. Внешний вид слайдов не изменился
  (попиксельное сравнение всех 36 слайдов).

## PR #5

- **Переключатель хранения данных:** в базе данных (MySQL) или в файловой структуре (`data/*.json`).
  Выбор при установке и в админке (Публикация → «Хранение данных») с переносом всех данных.
  Режим показан в шапке админки.
- Исправлено: в режиме базы данных не сохранялись отметки блюд «Акцент» и «New» (добавлена колонка `extra`,
  существующие базы обновляются автоматически).
- «+ Афиша» — выпадающий список: неделя или две недели, три вида.
- Размер текста на слайде и в «Шрифтах» — до 200%.
- Рамки с названием и описанием растут под длинный текст; описания видны во всех раскладках.

## PR #4

- Исправлено: после обновления сайта предпросмотр в админке мог перестать показывать слайды, потому что
  браузер смешивал новые скрипты со старыми из кеша. Теперь сборка добавляет `?v=<версия>` ко всем
  скриптам и стилям, а плеер не падает, если какого-то плагина GSAP нет.

## PR #3

- **Анимация**: расширены эффекты для изображений (9 вариантов появления, 6 постоянных), названий
  блюд (8 эффектов, в том числе ScrambleText) и цен (7 вариантов появления, в том числе счётчик
  и «штамп», плюс 6 постоянных), у каждой группы своя длительность.
- **Акцент на блюде** (4 стиля оформления и анимация цены) и **значок NEW** (звезда-стикер, 6 анимаций, свой текст).
- **9 раскладок**: только фото (1, 2, 3), фото + 1 текстом (1, 2, 3), фото + 2 текстом (1 — тарелка
  по центру, 2, 3). Всего 26 раскладок.
- **Слайд «Обратный отсчёт»** до мероприятия сегодня: название, время, живой счётчик, изображение.
- **Слайд «Объявление»**: 3 вида.
- **Размер элемента в %** в «Элементах слайда» (вдобавок к сдвигу X/Y).
- **Палитра цветов** (своя) и **цвета надписей** для 15 групп, глобально и на слайде.
- **Загрузка логотипа**; название и подпись перенесены в блок «Шапка экрана».
- **Вход в админку без базы** (admin / pass123, пароль меняется).
- Исправлено: мероприятие, заканчивающееся после полуночи, считалось завершённым.
- Исправлено: побуквенные эффекты больше не разрывают слова при переносе.
- **«+ Блюда» с выбором вида слайда** (26 раскладок в 3 группах) и автозаполнением блюдами.
- **Цвета и скорость анимированного градиента** (`settings.bg`, переопределение на слайде).
- Исправлено: длинные слова в рамках больше не переносятся посередине; цена в строке больше не обрезается справа.
- Документация вынесена в `docs/`, добавлены `CLAUDE.md` и генератор справочника `tools/docs.mjs`.

## PR #2

- Переключатели шапки (целиком и по частям), бегущей строки и полосы времени слайда.
- Лёгкий анимированный градиент на фоне слайдов: общий и на слайде.

## PR #1 — первая версия

- Плеер для вертикального FullHD: Swiper 14 + GSAP 3.15, холст 1080×1920 с адаптацией к вьюпорту.
- Стиль по мотивам присланного печатного меню: тёмный фон, обтравленные тарелки уходят за край,
  пунктир до цены, цена с весом, рамки со скруглением. Компоновки 1:2, 2:4, 3:6.
- Афиша на неделю и на 2 недели (до 7 событий), блок «Скоро у нас», расписание показа слайдов.
- Админка на Bootstrap 5, бэкенд PHP + MySQL, выгрузка по FTP, ZIP, Git, GitHub Pages.

## Принятые решения

- **Библиотеки лежат в репозитории**, CDN не используется: экран в кафе должен работать без интернета.
- **CSS Swiper переписан без nesting** (`vendor/swiper/swiper-flat.css`): встроенные браузеры ТВ
  не понимают `&`.
- **Calibri не распространяется** (лицензия Microsoft): используется системный, иначе Carlito.
- **Одна модель на всех** (`shared/model.js`): экран, админка и сборка не расходятся.
- **Публикация целиком** (полная замена данных в транзакции): проще и надёжнее построчных правок
  для объёмов кафе.
- **Анимация — на каждый показ слайда** (`gsap.context` + `revert`), чтобы ТВ-браузер не копил память
  за дни работы. Фоновые эффекты работают только у активного слайда.
